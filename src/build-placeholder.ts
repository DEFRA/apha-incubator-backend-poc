/**
 * Placeholder TypeScript module so the build pipeline (`tsc`, `docker compose build`) has
 * at least one real `.ts` input to emit into `dist/` before genuine `src` files are converted.
 * Remove once actual `.ts` source files exist (see conversion steps 03 onward).
 */
export const buildPlaceholder = true
